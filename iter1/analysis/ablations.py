"""Phase 5 — ablation analyses.

Currently a stub. Per protocol §"Phase 5: Statistical analysis", the headline
table is in headline_comparison.py. This module is reserved for follow-up
ablations the human author chooses to run (e.g., effect of archive cell count,
descriptor choice). Left as a stub on purpose — extending here without
protocol updates is scope creep.
"""
from __future__ import annotations


def todo() -> None:
    raise NotImplementedError(
        "ablations.py is a stub. Add specific ablations only after deciding to "
        "extend protocol scope. See critique.md for the candidates."
    )


if __name__ == "__main__":
    todo()
